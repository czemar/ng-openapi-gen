/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FooResponse } from '../../models/foo-response';

export interface GetFoos$Params {

/**
 * How many items to return at one time (max 100)
 */
  limit?: number;
}

export function getFoos(http: HttpClient, rootUrl: string, params?: GetFoos$Params, context?: HttpContext): Observable<StrictHttpResponse<FooResponse>> {
  const rb = new RequestBuilder(rootUrl, getFoos.PATH, 'get');
  if (params) {
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FooResponse>;
    })
  );
}

getFoos.PATH = '/foos';
