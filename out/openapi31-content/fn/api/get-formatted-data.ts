/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FormattedData } from '../../models/formatted-data';

export interface GetFormattedData$Params {
}

export function getFormattedData(http: HttpClient, rootUrl: string, params?: GetFormattedData$Params, context?: HttpContext): Observable<StrictHttpResponse<FormattedData>> {
  const rb = new RequestBuilder(rootUrl, getFormattedData.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FormattedData>;
    })
  );
}

getFormattedData.PATH = '/data-formats';
