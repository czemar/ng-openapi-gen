/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FileUpload } from '../../models/file-upload';
import { UploadResponse } from '../../models/upload-response';

export interface UploadFile$FormData$Params {
      body?: FileUpload
}

export function uploadFile$FormData(http: HttpClient, rootUrl: string, params?: UploadFile$FormData$Params, context?: HttpContext): Observable<StrictHttpResponse<UploadResponse>> {
  const rb = new RequestBuilder(rootUrl, uploadFile$FormData.PATH, 'post');
  if (params) {
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UploadResponse>;
    })
  );
}

uploadFile$FormData.PATH = '/upload';
