/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { StringConstraints } from '../../models/string-constraints';

export interface GetStringConstraints$Params {
}

export function getStringConstraints(http: HttpClient, rootUrl: string, params?: GetStringConstraints$Params, context?: HttpContext): Observable<StrictHttpResponse<StringConstraints>> {
  const rb = new RequestBuilder(rootUrl, getStringConstraints.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StringConstraints>;
    })
  );
}

getStringConstraints.PATH = '/string-constraints';
