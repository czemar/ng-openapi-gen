/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { NumericTypes } from '../../models/numeric-types';

export interface GetNumericTypes$Params {
}

export function getNumericTypes(http: HttpClient, rootUrl: string, params?: GetNumericTypes$Params, context?: HttpContext): Observable<StrictHttpResponse<NumericTypes>> {
  const rb = new RequestBuilder(rootUrl, getNumericTypes.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<NumericTypes>;
    })
  );
}

getNumericTypes.PATH = '/numeric-types';
