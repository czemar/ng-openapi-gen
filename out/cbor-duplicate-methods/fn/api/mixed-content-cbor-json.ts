/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResponseModel } from '../../models/response-model';
import { SearchRequest } from '../../models/search-request';

export interface MixedContent$Cbor$Json$Params {
      body: SearchRequest
}

export function mixedContent$Cbor$Json(http: HttpClient, rootUrl: string, params: MixedContent$Cbor$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<ResponseModel>> {
  const rb = new RequestBuilder(rootUrl, mixedContent$Cbor$Json.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/cbor');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResponseModel>;
    })
  );
}

mixedContent$Cbor$Json.PATH = '/mixed-content';
