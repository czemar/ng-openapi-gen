/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Product1 } from '../../models/product-1';
import { Product2 } from '../../models/product-2';
import { Product3 } from '../../models/product-3';
import { Product4 } from '../../models/product-4';

export interface ProductsGet$Params {
}

export function productsGet(http: HttpClient, rootUrl: string, params?: ProductsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<(Product1 | Product2 | Product3 | Product4)>> {
  const rb = new RequestBuilder(rootUrl, productsGet.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<(Product1 | Product2 | Product3 | Product4)>;
    })
  );
}

productsGet.PATH = '/products';
