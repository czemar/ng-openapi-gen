/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PetstorePetsModel } from '../../models/petstore-pets-model';

export interface ListPets$Params {

/**
 * How many items to return at one time (max 100)
 */
  limit?: number;
}

export function listPets(http: HttpClient, rootUrl: string, params?: ListPets$Params, context?: HttpContext): Observable<StrictHttpResponse<PetstorePetsModel>> {
  const rb = new RequestBuilder(rootUrl, listPets.PATH, 'get');
  if (params) {
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PetstorePetsModel>;
    })
  );
}

listPets.PATH = '/pets';
