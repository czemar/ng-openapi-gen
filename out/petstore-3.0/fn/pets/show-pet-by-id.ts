/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PetstorePetsModel } from '../../models/petstore-pets-model';

export interface ShowPetById$Params {

/**
 * Pet's id to retrieve
 */
  petId: string;
}

export function showPetById(http: HttpClient, rootUrl: string, params: ShowPetById$Params, context?: HttpContext): Observable<StrictHttpResponse<PetstorePetsModel>> {
  const rb = new RequestBuilder(rootUrl, showPetById.PATH, 'get');
  if (params) {
    rb.path('petId', params.petId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PetstorePetsModel>;
    })
  );
}

showPetById.PATH = '/pets/{petId}';
