/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WebhookPayload } from '../../models/webhook-payload';

export interface GetWebhookPayload$Params {
}

export function getWebhookPayload(http: HttpClient, rootUrl: string, params?: GetWebhookPayload$Params, context?: HttpContext): Observable<StrictHttpResponse<WebhookPayload>> {
  const rb = new RequestBuilder(rootUrl, getWebhookPayload.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WebhookPayload>;
    })
  );
}

getWebhookPayload.PATH = '/webhook-payload';
