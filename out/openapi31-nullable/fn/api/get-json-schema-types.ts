/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { JsonSchemaTypes } from '../../models/json-schema-types';

export interface GetJsonSchemaTypes$Params {
}

export function getJsonSchemaTypes(http: HttpClient, rootUrl: string, params?: GetJsonSchemaTypes$Params, context?: HttpContext): Observable<StrictHttpResponse<JsonSchemaTypes>> {
  const rb = new RequestBuilder(rootUrl, getJsonSchemaTypes.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<JsonSchemaTypes>;
    })
  );
}

getJsonSchemaTypes.PATH = '/jsonschema-types';
