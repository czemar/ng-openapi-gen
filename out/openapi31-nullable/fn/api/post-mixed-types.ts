/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MixedTypesInput } from '../../models/mixed-types-input';
import { MixedTypesOutput } from '../../models/mixed-types-output';

export interface PostMixedTypes$Params {
      body?: MixedTypesInput
}

export function postMixedTypes(http: HttpClient, rootUrl: string, params?: PostMixedTypes$Params, context?: HttpContext): Observable<StrictHttpResponse<MixedTypesOutput>> {
  const rb = new RequestBuilder(rootUrl, postMixedTypes.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MixedTypesOutput>;
    })
  );
}

postMixedTypes.PATH = '/mixed-types';
