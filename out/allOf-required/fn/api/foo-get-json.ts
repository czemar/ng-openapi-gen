/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse, HttpHeaders } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Person } from '../../models/person';

export interface FooGet$Json$Params {
}

export function fooGet$Json(http: HttpClient, rootUrl: string, params?: FooGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Person>> {
  const rb = new RequestBuilder(rootUrl, fooGet$Json.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Person>;
    })
  );
}

fooGet$Json.PATH = '/foo';
